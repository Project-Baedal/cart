spring:
  redis:
    host: localhost
    port: 6379
    password: # your-redis-password (???)
    timeout: 2000 # 2?
    jedis:
      pool:
        max-active: 10 # ?? ?? ?
        max-idle: 5 # ?? ?? ?? ?
        min-idle: 1 # ?? ?? ?? ?
        max-wait: -1 # ?? ?? ?? (ms)
    # Lettuce ?? ??
    lettuce:
      pool:
        max-active: 10 # Lettuce? ??? ?? ??? ? ??
        max-idle: 5
        min-idle: 1
        max-wait: -1