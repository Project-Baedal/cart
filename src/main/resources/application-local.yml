server:
  port: 8083

spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5434/cart
#    username: cart
#    password: cart
#    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    com.baedal: DEBUG